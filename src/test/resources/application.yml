app:
  datasource:
    main:
      driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
      jdbc-url: jdbc:tc:postgresql:14:///timetable_db
      username: test
      password: test
      pool-size: 5

spring:
  flyway:
    locations: classpath:db.migration